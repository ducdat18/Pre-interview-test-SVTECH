---
# Ansible collection dependencies.
# Both ship with the full `ansible` package; install explicitly with:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix     # mount, authorized_key
    version: ">=1.5.0"
  - name: community.general # (optional helpers)
    version: ">=8.0.0"
